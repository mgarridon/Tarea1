
<!-- Main content -->
<div class="box">
  <div class="box-header">
    <h3 class="box-title">Listado Ventas
      <%= link_to 'Agregar Venta', nueva_venta_path,:class =>'btn bg-olive margin' %></h3>
  </div>
  <!-- /.box-header -->
  <div class="box-body">
    <div class="table-responsive">
      <table id="example1" class="table table-bordered table-striped">
        <thead>
        <tr>
          <th>Fecha</th>
          <th>Cantidad</th>
          <th>Sub Total</th>
          <th>Total</th>
          <th>Id Cliente</th>
          <th>Id Empleado</th>
          <th>Id Producto</th>
          <th>Mostrar</th>
          <th>Editar</th>
          <th>Borrar</th>
        </tr>
        </thead>
        <tbody>
        <% @venta.each do |actual| %>
            <tr>
              <td><%= actual.fecha %></td>
              <td><%= actual.cantidad %></td>
              <td><%= actual.subtotal %></td>
              <td><%= actual.total %></td>
              <td><%= actual.id_cliente%></td>
              <td><%= actual.id_empleado%></td>
              <td><%= actual.id_producto%></td>
              <td><%= link_to '<i class="glyphicon glyphicon-book"></i>'.html_safe,  venta_path(actual), class: 'btn btn-success' %></td>
              <td><%= link_to '<i class="glyphicon glyphicon-pencil"></i>'.html_safe,  editar_venta_path(actual), class: 'btn btn-info' %></td>
              <td><%= link_to '<i class="glyphicon glyphicon-trash"></i>'.html_safe, ventas_path(actual), method: :delete, data: { confirm: '¿Está seguro?' }, class: 'btn btn-danger' %></td>

            </tr>
        <%end%>
        </tbody>
        <tfoot>
        </tfoot>
      </table>
    </div>

  </div>
  <!-- /.box-body -->
</div>
<!-- /.box -->
</div>
<!-- /.col -->
</div>
<!-- /.row -->
</section>
<!-- /.content -->
</div>

<script>

$(document).ready(function() {
  $('#example1').DataTable({
    "language": {
      "url": "//cdn.datatables.net/plug-ins/1.10.15/i18n/Spanish.json"
    }
  });
});

    $(function () {
        $('#example1').DataTable()
        $('#example2').DataTable({
            'paging'      : true,
            'lengthChange': false,
            'searching'   : false,
            'ordering'    : true,
            'info'        : true,
            'autoWidth'   : false
        })
    })
</script>